[pytest]
# asyncio_mode=auto: o pytest-asyncio trata testes e fixtures async
# automaticamente, sem precisar decorar cada um. Sem isso, o erro é
# "async def functions are not natively supported" (teste vira skip/erro).
asyncio_mode = auto
testpaths = tests
