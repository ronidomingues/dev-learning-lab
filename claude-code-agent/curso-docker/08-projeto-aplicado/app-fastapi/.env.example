# Copie para .env e ajuste. O .env NUNCA vai para o git nem para a imagem.
APP_ENV=development
APP_VERSION=dev

# SQLite (padrão, sem infra):
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Postgres (compose):
# DATABASE_URL=postgresql+asyncpg://appuser:trocar-esta-senha@db:5432/appdb
