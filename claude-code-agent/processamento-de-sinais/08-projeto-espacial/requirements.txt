# Dependências do projeto `cosmos`.
# Versões com as quais foi executado e verificado em 19/08/2026.
numpy>=1.24,<3
scipy>=1.10        # usado só em deteccao.py (erfc e a inversa da gaussiana)
matplotlib>=3.6    # opcional: só para os subcomandos com --figuras
